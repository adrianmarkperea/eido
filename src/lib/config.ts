import { dev } from '$app/environment'

export const title = 'Mark Perea'
export const subtitle = 'writing (about) software because it\'s fun'
export const url = dev ? 'http://localhost:5173' : 'https://markperea.com'

